using System;

internal static class ProgramHelpers
{

    static void Main(string[] args, )
    {
        //Viet ctrinh tinh tong 3 so

        Tinhtong3so(a, b, c);
        Tinhtich3so(a, b, c);
        Console.ReadKey();
    }
}